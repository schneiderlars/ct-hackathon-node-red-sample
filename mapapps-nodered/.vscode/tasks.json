{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Jetty Server",
            "type": "shell",
            "command": "mvn jetty:run -P'watch-all,include-mapapps-deps'",
            "group": "build"
        }
    ]
}